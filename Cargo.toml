[package]

name = "session"
version = "0.2.0"
authors = ["Zach Pomerantz <zmp@umich.edu>"]
keywords = ["iron", "web", "http", "session"]
license = "MIT"
description = "A session middleware for Iron."

[lib]

name = "session"
path = "src/lib.rs"

[dependencies]
iron = "0.4.0"
